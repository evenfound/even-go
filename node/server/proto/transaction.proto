syntax = "proto3";
package api;

message GetTransactionRequestMessage {
    string address = 1;
}
message GetTransactionResponseMessage {
    string transaction = 1;
}

service Transaction {
    rpc GetTransactions (GetTransactionRequestMessage) returns (GetTransactionResponseMessage) {
    }
}